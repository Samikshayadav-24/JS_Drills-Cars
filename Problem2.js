// ==== Problem #2 ====
// The dealer needs the information on the last car in their inventory.
// Execute a function to find what the make and model of the last car 
//in the inventory is?  Log the make and model into the console in 
//the format of: 

    const lastId = function(inventory)    
    {
        return lastElement = inventory[inventory.length - 1];
    } 
    //console.log(lastId(inventory));
    module.exports = {
        lastId 
   };
    
 